name: 🚀 Pipeline CI/CD Criativo

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    name: 🔧 Construção e Testes
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout do código
        uses: actions/checkout@v4

      - name: ⚙️ Configurar ambiente Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: 📦 Instalar dependências
        run: |
          if [ -f "package.json" ]; then
            npm install
          else
            echo "⚠️ Nenhum package.json encontrado."
          fi

      - name: 🧪 Executar testes
        run: npm test

      - name: 🏗️ Criar build fictício
        run: |
          mkdir -p dist
          echo "Build gerado com sucesso em $(date)" > dist/build-info.txt

      - name: 📤 Publicar artefato
        uses: actions/upload-artifact@v4
        with:
          name: build-gerado
          path: dist/

      - name: 🎉 Conclusão
        run: echo "✅ Pipeline finalizado com sucesso! Tudo funcionando perfeitamente. ✨"
